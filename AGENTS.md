# AGENTS.md

本仓库是个人抽出来的通用组件合集（存档用途，非运行时；组件彼此独立，无顶层构建）。只维护以下少量编辑规则。

## 编辑规则

- **先读再改、最小连贯改动**：改某组件前先读它的 README（`using-browser` 还要读 `SKILL.md` 与 `references/`）；在拥有该职责的文件里改，不跨组件复制内容。
- **保持组件独立**：不引入跨组件 import/调用或共享依赖；不新增根 `package.json` 或 workspace，除非用户明确要求。
- **改动默认不提交、等 review；提交时在主分支直接提交并推送**：做完改动默认不 `commit`、不 `push`，留在工作树等用户 review。只有当用户明确说要提交时，才直接在主分支 `commit` 并 `push`，并在提交信息里**详细说明改动**（改了哪些组件/文件、做了什么、为什么）。
- **统一 License**：整个仓库用顶层 `LICENSE`（MIT），不给组件单独加 LICENSE。
